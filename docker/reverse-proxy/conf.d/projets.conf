# docker/reverse-proxy/conf.d/projects.conf
# -------------------------------------------------
######################## project1 ########################
upstream project1_web { server 10.77.11.10:80; }   # IP CT web
server {
    listen 80;
    server_name project1.local;
    location / {
        proxy_pass http://project1_web;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

######################## project2 ########################
upstream project2_web { server 10.77.22.10:80; }
server {
    listen 80;
    server_name project2.local;
    location / { proxy_pass http://project2_web; … }
}

######################## projectN ########################
upstream projectN_web { server 10.77.33.10:80; }
server {
    listen 80;
    server_name projectN.local;
    location / { proxy_pass http://projectN_web; … }
}
